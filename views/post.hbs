<div class="row mt-5">
  <div class="col-md-6 m-auto">
    <div class="card card-body">
      {{#if post}}
      <p>
        <h1>{{post.title}}</h1>
        <a href="https://twitter.com/{{post.handle}}"
        {{#each post.includedTweets}}
          <p>{{this.created_at}}</p>
          <p>{{this.full_text}}</p>
          <a href={{this.url}}>{{this.url}}</a>
          <br>
        {{/each}}
      </p>
      {{else}}
        <p>post not found</p>
      {{/if}}

    </div>
  </div>
</div>
