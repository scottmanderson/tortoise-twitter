{{#if post}}
<div>
  <h1>{{post.title}}</h1>
  <a href="https://twitter.com/{{post.handle}}">https://twitter.com/{{post.handle}}</a>
  {{#each post.includedTweets}}
    <p>{{this.created_at}}</p>
    <p>{{this.full_text}}</p>
    <a href={{this.url}}>{{this.url}}</a>
    <hr />
  {{/each}}
</div>
{{else}}
  <p>post not found</p>
{{/if}}